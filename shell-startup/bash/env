# WARNING: this will $BASH_ENV to get a correct startup sequence
. ~/.dotfiles/shell-startup/shell/env

# Case-insensitive globbing (used in pathname expansion)
shopt -s nocaseglob

# Autocorrect typos in path names when using `cd`
shopt -s cdspell
